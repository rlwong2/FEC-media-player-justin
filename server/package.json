{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db-create": "mysql -u root < seeds/media-schema.sql",
    "seed": "mysql -u root media < seeds/InsertData.sql",
    "server": "nodemon index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mysql": "^2.18.1",
    "s3": "^4.4.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
